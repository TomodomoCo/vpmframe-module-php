;;;;;;;; User, Group and Listen ;;;;;;;;

; application credentials
user = <%= app_user %>
group = <%= app_group %>

; listen on sock
listen = /var/run/php5-fpm-pools-<%= app_name %>.sock

chdir = /home/<%= app_user %>
